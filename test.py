import time
from datetime import datetime

# def generate_unique_id():
#     timestamp = str(int(time.time()))  # Using milliseconds for uniqueness
#     # You can add more information to make it unique, like user-specific data
#     unique_id = f"ID_{timestamp}"
#     return unique_id

# print(generate_unique_id())
# print(time.time())
print(datetime.now().year)